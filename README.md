# Typst Packages

Typst packages by Patrick Fürderer-Tosolini / Hyperrick.

## Packages

- [`framefit`](framefit/) - fit text into fixed frames by adjusting text size.

Each package lives in its own subfolder with its own `typst.toml`, `README.md`,
examples, and tests.

